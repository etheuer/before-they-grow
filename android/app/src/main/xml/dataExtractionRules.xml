<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
  <!-- API 31+ device-to-device transfer: no family-bearing content is
       transferred. allowBackup = false in the manifest already disables
       cloud backup; this rule closes the device-transfer path. -->
  <cloud-backup disableIfNoEncryptionCapabilities="false">
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
  </cloud-backup>
  <device-transfer>
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
  </device-transfer>
</data-extraction-rules>