## Standards

**Hard from 02dfd99**

1. **Fake honesty — fixed.** `sqliteMemoryRepository.test.ts` now snapshots state, rolls back if the txn block throws, keeps the row only for `postCommitFailure` after a completed block, stores `created_at`, and sorts `ORDER BY created_at ASC`. New test: rollback leaves zero rows.

2. **`App.test.tsx` 656 — not fixed.** Still titled “duplicative (indeterminate)” and still drives `not-saved`. 2428e78 does not touch that file.

**Judgement leftovers:** `saveReliability.test.ts:414` is still named “reports a conflict and never overwrites” but now asserts Saved + transcript update. Dead types (`SaveConflictError`, `isSaveIndeterminate`, `operation-conflict`) are gone. `reconcileSaveOperations` comment still says it “never creates a catalog row” while the body calls `repository.create`.

## Spec

**Idempotent retry works.** Same identity + media present skips `commit`, continues to SQLite `create`, one row. `reliablySaveMemory` (`saveReliability.ts:404–456`) uses `reconcileFinal` before `commit`. Covered by `continues a prepared journal on same-identity retry after media is present` (Saved, `committed === []`, one memory, journal cleared). `expoMediaStore.commit` still throws if invoked on an existing final; the retry path no longer invokes it in that case.

**Conflict does not overwrite identity or media.** Journal `prepare` conflicts on identity/content mismatch (adapter test). `create` only INSERTs. Voice text may change via `updateReviewedTranscript` only. Different `mediaSha256` is still Not saved (`capture.ts:182–183`). Reconcile conflict is a per-operation result, not an all-family `SaveReconciliationError`.

**Also fixed:** pre/post-commit via `verifyMemoryVisible` after a txn throw; manual Indeterminate now sets `indeterminateStorage`.

**Still open (not claimed):** interruption copy for reconciled Not saved; AC9/AC10 matrix; no application-level test that a different media hash is a conflict.

Hard finding 1 is fixed; hard finding 2 is not. Claimed retry/conflict paths hold; nothing new is broken there.

**Standards: 2 leftover (1 hard). Worst: App.test still misnamed and never mounts Indeterminate.**
**Spec: retry + no-overwrite hold. Worst leftover: reconcile comment vs create, and no hash-conflict test.**
