`npm run check`: **green** (114 tests; typecheck/build pass). Commit changes 19 files.

AC review:
- **Pass:** 1, 3, 4, 6, 8.
- **Partial:** 2, 5, 7.
- **Fail:** 9.

Gaps/bugs:
- Individual deletion checkpoints WAL **before** clearing the deletion journal; `clear()` then writes to WAL with no final checkpoint. Absence verification checks only the memory row, not media/file or journal absence.
- Delete-all capture-cache cleanup is best-effort and excluded from `verifyWiped()`, so deletion can report success with cache remnants; failures do not block/retry. It also deletes every `.m4a` in the app cache, risking unrelated resources.
- Failure/process-death tests do not cover every boundary (notably marker/close/clear failures, deletion phase advances, and cache cleanup), so AC9 is overstated.
- No obvious product creep; scope remains deletion-related.

**Needs fixes — do not close as-is.**
